telegram-bot:
  build: .
  container_name: telegram-knowledge-bot
  volumes:
    - ./knowledge_base:/app/knowledge_base
    - ./user_queries:/app/user_queries
    - ./chroma_db:/app/chroma_db
    - ./.env:/app/.env:ro
  restart: unless-stopped
